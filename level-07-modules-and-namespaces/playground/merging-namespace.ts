namespace Utils {
	export function add(a: number, b: number) {
		return a + b;
	}
}

namespace Utils {
	export function subtract(a: number, b: number) {
		return a - b;
	}
}

Utils.add(2, 2);
Utils.subtract(3, 2);
